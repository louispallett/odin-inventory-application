extends layout

block content 
    h1=title
    hr
    p #[b Click] on an category to show all information about it, including it's country of origin and category.
    div(class="content-wrapper")
        if category_list.length 
            each category in category_list
                a(href=category.url class="list-item")
                    p=category.name